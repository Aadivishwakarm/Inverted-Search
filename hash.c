// #include<stdio.h>
// #include<stdlib.h>
#include "head.h"

int create_hash(file_list **arr, int size)
{
    for (int i = 0; i < size; i++)
    {
        arr[i] = NULL;
    }
    return 0;
}
